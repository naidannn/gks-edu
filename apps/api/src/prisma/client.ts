/**
 * Single import point for the generated Prisma Client.
 *
 * Prisma 7 emits the client into `src/generated/prisma` instead of
 * `node_modules/@prisma/client`; re-exporting it here keeps the rest of the
 * codebase free of deep relative paths into generated code.
 */
export * from '../generated/prisma/client.js';
